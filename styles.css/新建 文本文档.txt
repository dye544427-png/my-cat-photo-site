:root{
  --max-width:1200px;
  --accent:#111;
  --muted:#666;
  --bg:#fff;
  --gap:1rem;
  --radius:6px;
  --btn-bg:#111;
  --btn-color:#fff;
  font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  color-scheme: light;
}
*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  background:var(--bg);
  color:var(--accent);
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
  line-height:1.5;
}

/* Layout */
.container{
  max-width:var(--max-width);
  margin:0 auto;
  padding:2rem;
}

/* Header / Nav */
.site-header{border-bottom:1px solid #eee}
.nav{display:flex;align-items:center;justify-content:space-between;gap:1rem}
.logo{font-weight:700;text-decoration:none;color:var(--accent);font-size:1.1rem}
.nav-links{list-style:none;margin:0;padding:0;display:flex;gap:1rem}
.nav-links a{text-decoration:none;color:var(--muted)}

/* Hero */
.hero{display:grid;grid-template-columns:1fr 280px;align-items:center;gap:2rem;padding:3rem 0}
.hero-content h1{font-size:2rem;margin:0 0 .5rem}
.lead{color:var(--muted);margin:0 0 1rem}
.btn{
  display:inline-block;
  background:var(--btn-bg);
  color:var(--btn-color);
  padding:.6rem 1rem;
  border-radius:999px;
  text-decoration:none;
  font-weight:600;
}
.hero-image{
  height:220px;
  border-radius:10px;
  background-image:linear-gradient(120deg, #1110 0%, #1110 100%), url('images/hero.jpg');
  background-size:cover;background-position:center;
  box-shadow:0 8px 24px rgba(0,0,0,.08);
}

/* About */
.about h2,.portfolio h2,.contact h2{margin-top:0}

/* Filters */
.filters{display:flex;gap:.5rem;margin:1rem 0 1.5rem;flex-wrap:wrap}
.filter-btn{
  background:transparent;border:1px solid #ddd;padding:.45rem .6rem;border-radius:999px;cursor:pointer;
}
.filter-btn.active{background:var(--btn-bg);color:var(--btn-color);border-color:var(--btn-bg)}

/* Grid gallery */
.grid{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:var(--gap);
}
.grid-item{
  position:relative;
  overflow:hidden;
  border-radius:8px;
}
.grid-item img{
  width:100%;height:100%;object-fit:cover;display:block;transition:transform .4s ease;
}
.grid-item:hover img{transform:scale(1.05)}
.grid-item figcaption{
  position:absolute;left:0;right:0;bottom:0;background:linear-gradient(180deg,transparent,rgba(0,0,0,.5));
  color:#fff;padding:.6rem;font-size:.9rem;
}

/* Lightbox */
.lightbox{
  position:fixed;inset:0;display:flex;align-items:center;justify-content:center;
  background:rgba(0,0,0,.85);z-index:1000;opacity:0;pointer-events:none;transition:opacity .2s ease;
}
.lightbox[aria-hidden="false"]{opacity:1;pointer-events:auto}
.lightbox-content{max-width:90vw;max-height:80vh;text-align:center}
.lightbox img{max-width:100%;max-height:70vh;border-radius:6px}
.lightbox-caption{color:#ccc;margin-top:.5rem}
.lightbox-close, .lightbox-prev, .lightbox-next{
  position:absolute;background:transparent;border:0;color:#fff;font-size:2rem;padding:.5rem;cursor:pointer;
}
.lightbox-close{right:1rem;top:1rem}
.lightbox-prev{left:1rem;top:50%;transform:translateY(-50%)}
.lightbox-next{right:1rem;top:50%;transform:translateY(-50%)}

/* Contact */
.contact-form{max-width:640px;display:grid;gap:.75rem}
.contact-form label{display:block;font-size:.9rem;color:var(--muted)}
.contact-form input, .contact-form textarea{
  width:100%;padding:.6rem;border:1px solid #e6e6e6;border-radius:6px;font-size:1rem;
}
.form-actions{display:flex;align-items:center;gap:1rem}
.hint{color:var(--muted);font-size:.85rem}

/* Footer */
.site-footer{border-top:1px solid #eee;padding:2rem;text-align:center;color:var(--muted);font-size:.9rem}

/* Responsive */
@media (max-width:900px){
  .grid{grid-template-columns:repeat(2,1fr)}
  .hero{grid-template-columns:1fr;gap:1rem}
}
@media (max-width:600px){
  .grid{grid-template-columns:1fr}
  .nav-links{display:none}
  .hero-image{height:160px}
}
.hidden{display:none !important}